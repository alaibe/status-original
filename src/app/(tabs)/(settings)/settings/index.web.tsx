import { EmptyPane } from '@/features/navigation/empty-pane';

/** The sections are in the sidebar; the pane shows whichever one is open. */
export default function SettingsScreen() {
  return <EmptyPane hint="Choose a setting" />;
}
