# Sends TEXT from the composer, then taps the gutter beside the reply to dismiss the keyboard.
appId: com.statusoriginal.app
---
- tapOn:
    id: "composer-input"
- inputText: ${TEXT}
- tapOn:
    id: "composer-send"
- tapOn:
    point: "95%,40%"
