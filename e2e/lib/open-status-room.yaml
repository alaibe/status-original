# Launches, lets the chat list settle, and opens the local Status room through its app link.
appId: com.statusoriginal.app
---
- launchApp
- extendedWaitUntil:
    visible:
      id: "conversation-local-status"
    timeout: 15000
- waitForAnimationToEnd:
    timeout: 5000
- openLink: "statusoriginal://chat/local-status"
