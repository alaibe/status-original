# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Keep comments to the minimum. Add one only when it explains something the code cannot make clear; never restate the code or add tautological commentary.
